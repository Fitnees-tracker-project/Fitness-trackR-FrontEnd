import React from 'react';
import { useOutletContext, Link, useParams } from 'react-router-dom';

const Routines = () => {
    return(
        <div>
            <p>These are Routines</p>
        </div>
    )
}

export default Routines;